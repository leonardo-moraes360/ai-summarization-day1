[project]
name = "ai-summarization"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "markrender>=1.0.7",
    "openai>=3.14.0",
]

[project.scripts]
ai-summarization = "ai_summarization:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"
